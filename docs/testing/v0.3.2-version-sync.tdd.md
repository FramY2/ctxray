# v0.3.2 runtime version synchronization — TDD evidence

## User journey

> As a CtxWise user, I want every runtime surface to report the installed npm
> version, so that diagnostics and reproducibility metadata identify the code I
> am actually running.

## RED / GREEN record

| Stage           | Command                                                                                                                                          | Result                    | Evidence                                                                                                                                            |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| RED             | `npm test -- tests/e2e/cli.test.ts tests/integration/lockfile.test.ts --maxWorkers=1`                                                            | Expected version failures | CLI and lockfile returned `0.3.0` while `package.json` was `0.3.1`. One unrelated existing process also hit the workstation's `VirtualAlloc` limit. |
| GREEN (focused) | `npm test -- tests/e2e/cli.test.ts tests/integration/lockfile.test.ts --maxWorkers=1 --testNamePattern="reports the version\|hashes a redacted"` | PASS                      | Both newly failing guarantees passed.                                                                                                               |
| GREEN (files)   | `npm test -- tests/integration/lockfile.test.ts --maxWorkers=1` and `npm test -- tests/e2e/cli.test.ts --maxWorkers=1`                           | PASS                      | 4/4 integration and 7/7 end-to-end tests passed.                                                                                                    |
| Coverage        | `npm run test:coverage -- --maxWorkers=1`                                                                                                        | PASS                      | 17 files and 84 tests passed; 92.09% statements, 83.25% branches, 91.97% functions, and 94.79% lines.                                               |

## Guarantees

| #   | What is guaranteed                                                         | Test                                    | Type        | Result |
| --- | -------------------------------------------------------------------------- | --------------------------------------- | ----------- | ------ |
| 1   | `ctxwise --version` equals `package.json.version`.                         | `tests/e2e/cli.test.ts`                 | End to end  | PASS   |
| 2   | A default capability lock records the package version.                     | `tests/integration/lockfile.test.ts`    | Integration | PASS   |
| 3   | CLI, app-server metadata, and lockfiles import one runtime version source. | `src/version.ts` plus type/build checks | Integration | PASS   |

## Checkpoints

- RED: `6595b0f` — `test: add RED coverage for package version drift`
- GREEN: `b34a717` — `fix: derive runtime version from package metadata`

The GitHub release gate runs the standard parallel suite on Node.js 20, 22, and
24 after these local checks.
