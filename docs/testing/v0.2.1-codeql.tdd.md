# v0.2.1 CodeQL hardening TDD evidence

## Source and journey

The journey was derived from GitHub CodeQL alert 24: as a user inspecting
prompt metadata, I want arbitrary role names to remain inert data so that a
crafted `__proto__` role cannot alter aggregation behavior.

## RED

- Test: `tests/unit/xray.test.ts` — prototype-like role names are ordinary keys.
- Command: `npm test -- tests/unit/xray.test.ts`
- Result: 1/4 failed because `__proto__` was not an own property.
- Checkpoint: `586426c test: reproduce prototype-like prompt roles`

## GREEN

- Change: aggregate role summaries with `Map` and convert after aggregation.
- Commands: `npm test -- tests/unit/xray.test.ts`; `npm run typecheck`
- Result: 4/4 tests passed and TypeScript reported no errors.
- Checkpoint: `9eb506e fix: isolate prompt role aggregation`

## Guarantees

| #   | What is guaranteed                                                         | Test                | Type                | Result |
| --- | -------------------------------------------------------------------------- | ------------------- | ------------------- | ------ |
| 1   | `__proto__` is returned as an own role-summary key                         | `xray.test.ts`      | Security regression | PASS   |
| 2   | Other inherited-looking names such as `toString` remain ordinary summaries | `xray.test.ts`      | Security regression | PASS   |
| 3   | Prompt text is still absent from the generated X-Ray report                | existing X-Ray test | Privacy regression  | PASS   |

The complete coverage suite and a fresh CodeQL scan are required before the
v0.2.1 release is accepted.
